2073. Time Needed to Buy Tickets

Link --> https://leetcode.com/problems/time-needed-to-buy-tickets/submissions/1899640994/

-------
Code
------

class Solution {
    public int timeRequiredToBuy(int[] tickets, int k) {
        Queue<int[]> q = new LinkedList<>();
        for(int i=0; i<tickets.length; i++){
            q.add(new int[]{i, tickets[i]});
        }
        int cnt = 1;
        while(!q.isEmpty()){
            int ans[] = q.remove();
            ans[1]--;
            if(ans[0] == k && ans[1] == 0) return cnt;
            cnt++;
            if(ans[1] != 0) q.add(new int[]{ans[0], ans[1]});
        }
        return cnt;
    }
}

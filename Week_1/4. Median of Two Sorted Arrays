4. Median of Two Sorted Arrays

https://leetcode.com/problems/median-of-two-sorted-arrays/submissions/1884542811/

class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        int num[] = new int[nums1.length + nums2.length];
        for(int i=0; i<nums1.length; i++){
            num[i] = nums1[i];
        }
        for(int i=0; i<nums2.length; i++){
            num[i+nums1.length] = nums2[i];
        }
        Arrays.sort(num);
        double med = 0;
        if(num.length%2 == 0){
            med = (num[num.length/2] + num[(num.length/2)-1])/2.0;
        }else{
            med = num[num.length/2];
        }
        return med;
    }
}
